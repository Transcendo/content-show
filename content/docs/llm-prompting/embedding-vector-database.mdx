---
title: Embedding 和 Vector Database 解决什么
description: 理解向量和向量数据库如何支持语义检索。
---

Embedding 把文本、图片或其他对象变成向量，让系统能按语义相似度检索。Vector Database 用来存储、索引和查询这些向量。

它们服务语义检索，不是万能知识库。向量库只知道“哪些片段相似”，不保证片段真实、最新、完整，也不保证模型会正确使用检索结果。
